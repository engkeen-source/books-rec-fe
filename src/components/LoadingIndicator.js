import React from 'react';

const LoadingIndicator = () => (
  <div className="flex justify-center items-center">
    <span className="animate-pulse text-9xl">...</span>
  </div>
);

export default LoadingIndicator;
